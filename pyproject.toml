[project]
name = "agent-team"
version = "0.1.0"
description = "多智能体协作 Demo — ReAct Agent 团队 (架构师 → 程序员 ⇄ 测试员)"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]
